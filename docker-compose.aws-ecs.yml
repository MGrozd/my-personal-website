version: "3.8"

services:
  frontend:
    build:
      context: ./frontend
    image: ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/frontend:release-${GITHUB_SHA}

  backend:
    build:
      context: ./backend
    image: ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/backend:release-${GITHUB_SHA}

